// import the data from data.js
const tableData = data;

// Reference the HTML using d3
//      d3.select tells JS to look for <tbody> </tbody> tags in the HTML
var tbody = d3.select("tbpdy");

function buldTable(data) {

    
}
